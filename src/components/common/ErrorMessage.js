import styled from "styled-components"

const ErrorMessage = styled.span`
    display: block;
    width: auto;
    color: red;
    padding: 8px;
    margin: 8px;
    border: 1px solid rgb(255, 30, 30);
`

export {ErrorMessage}
