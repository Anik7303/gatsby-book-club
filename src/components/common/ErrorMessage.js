import styled from "styled-components"

const ErrorMessage = styled.span`
    display: block;
    width: auto;
    padding: 8px;
    margin: 8px;
    font-weight: bold;
    border: 1px solid rgb(255, 30, 30);
    color: rgb(255, 60, 30);
`

export { ErrorMessage }
